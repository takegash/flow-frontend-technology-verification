〇子供リストとは別に双方向リストを別に持つ
〇nodeに文を書ける
〇nodeの種類を選べる
〇分岐が書ける
〇統合が書ける
〇ifで右に行った先にifを書いたとき、
    2番branchから1番branchへの統合が上手くいかない
〇ボタンを実装できる
〇useStateに切り替え
〇ボタンを押すとnodeが出る
〇nodeごとに出るボタンの位置が変わる
〇生成時にちょうどいい位置に開始nodeを置く
--    (point, terminator, if)
〇nodeを消せる
〇edgeを書く

〇選択状態の表示
〇branchの値を更新
branchごとの幅を出したる
〇loopEndの上に統合しようとするとバグる
線の重なりを防止

〇nodesを更新する処理(createnodeなんか)
    で、nodesの内容を送信する処理を呼ぶ

〇受信時にsetNodes

DBにデータを入れる
ページ分け
ページ番号を登録するリクエストをRESTで実装
 or WebSocketに含める？
sessionIdとページ番号を対応させる
更新時は、対象ページと対応するidのみに配信

優先度低め
jsonで I/O
他ユーザの選択状態の制御変数はnodeに持たせる
branchの開始時点より上の場所に終了をもっていくことを防止する
    branchに開始地点のnodeの情報を持たせる？
pointの上でdelete押せないように
分岐後nodeが次の分岐の上にかぶる
    (分岐の終わりを書けば正常に表示できるはず)
